/**
 * Checks if value is less than or equal to other.
 *
 * @example
 * lte(1, 3) // true
 * lte(3, 3) // true
 * lte(3, 1) // false
 */
export function lte(value: number, other: number): boolean {
  return value <= other
}
