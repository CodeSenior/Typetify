/**
 * Checks if value is less than other.
 *
 * @example
 * lt(1, 3) // true
 * lt(3, 3) // false
 * lt(3, 1) // false
 */
export function lt(value: number, other: number): boolean {
  return value < other
}
