/**
 * Checks if value is greater than other.
 *
 * @example
 * gt(3, 1) // true
 * gt(3, 3) // false
 * gt(1, 3) // false
 */
export function gt(value: number, other: number): boolean {
  return value > other
}
